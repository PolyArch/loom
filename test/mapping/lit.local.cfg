import os

config.substitutions.insert(
    0,
    (
        "%loom-mapping-core-test",
        os.path.join(
            config.loom_obj_root, "test", "mapping", "loom-mapping-core-test"
        ),
    ),
)
