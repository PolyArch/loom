# REQUIRES: circt, verilator, yosys
# RUN: verilator --version | FileCheck %s --check-prefix=VERILATOR
# RUN: yosys -V | FileCheck %s --check-prefix=YOSYS-VERSION
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-integer-shift-test %t
# RUN: cd %t/artifacts && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench scalar_integer_shift.sv fixed_vector_integer_shift.sv fixed_vector_integer_shift_singleton.sv testbench.sv --Mdir obj_dir
# RUN: %t/artifacts/obj_dir/Vtestbench
# RUN: cd %t/artifacts && yosys -q -s portable_integer_shift.ys

# VERILATOR: Verilator 5.050
# YOSYS-VERSION: Yosys 0.67
