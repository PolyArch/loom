# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-switch-registered-grant-test %t disjoint
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module switch_registered_grant_disjoint_testbench switch_registered_grant_disjoint_module.sv switch_registered_grant_disjoint_testbench.sv --Mdir obj
# RUN: timeout %loom-timeout-xlong %t/obj/Vswitch_registered_grant_disjoint_testbench
# RUN: cd %t && yosys -q -s switch_registered_grant_disjoint.ys
