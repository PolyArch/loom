# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-switch-idle-presentation-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module switch_idle_presentation_disjoint_testbench switch_idle_presentation_disjoint_module.sv switch_idle_presentation_disjoint_testbench.sv --Mdir obj_disjoint
# RUN: timeout %loom-timeout-xlong %t/obj_disjoint/Vswitch_idle_presentation_disjoint_testbench
# RUN: cd %t && yosys -q -s switch_idle_presentation_disjoint.ys
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module switch_idle_presentation_contending_testbench switch_idle_presentation_contending_module.sv switch_idle_presentation_contending_testbench.sv --Mdir obj_contending
# RUN: timeout %loom-timeout-xlong %t/obj_contending/Vswitch_idle_presentation_contending_testbench
# RUN: cd %t && yosys -q -s switch_idle_presentation_contending.ys
