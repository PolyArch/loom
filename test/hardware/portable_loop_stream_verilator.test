# REQUIRES: circt, verilator
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-loop-stream-test %t
# RUN: verilator --version | FileCheck %s --check-prefix=VERILATOR
# RUN: cd %t/artifacts && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module leaf_testbench loop_stream_leaf.sv leaf_testbench.sv --Mdir leaf_obj_dir
# RUN: %t/artifacts/leaf_obj_dir/Vleaf_testbench
# RUN: cd %t/artifacts && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench loop_stream_system.sv testbench.sv --Mdir obj_dir
# RUN: %t/artifacts/obj_dir/Vtestbench

# VERILATOR: Verilator 5.050
