# REQUIRES: circt, verilator
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-integer-logic-test %t
# RUN: verilator --version | FileCheck %s --check-prefix=VERILATOR
# RUN: cd %t/tool-artifacts && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench scalar_integer_logic.sv fixed_vector_integer_logic.sv testbench.sv --Mdir obj_dir
# RUN: %t/tool-artifacts/obj_dir/Vtestbench

# VERILATOR: Verilator 5.050
