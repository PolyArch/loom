# REQUIRES: circt, verilator
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-common-skeleton-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL --Wno-PINMISSING -CFLAGS '-std=c++20' --top-module spatial_hierarchy_testbench spatial_hierarchy_module.sv spatial_hierarchy_testbench.sv --Mdir obj_spatial_hierarchy
# RUN: timeout %loom-timeout-xlong %t/obj_spatial_hierarchy/Vspatial_hierarchy_testbench
