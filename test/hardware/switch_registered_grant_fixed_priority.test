# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-switch-registered-grant-test %t fixed_priority
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module switch_registered_grant_fixed_priority_testbench switch_registered_grant_fixed_priority_module.sv switch_registered_grant_fixed_priority_testbench.sv --Mdir obj
# RUN: timeout %loom-timeout-xlong %t/obj/Vswitch_registered_grant_fixed_priority_testbench
# RUN: cd %t && yosys -q -s switch_registered_grant_fixed_priority.ys
