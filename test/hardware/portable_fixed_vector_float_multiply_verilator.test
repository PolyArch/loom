# REQUIRES: circt, verilator
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-float-multiply-test %t
# RUN: cd %t/generated && verilator --binary --timing --Wall --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL -DTEST_VECTOR -CFLAGS '-std=c++20' --top-module testbench fixed_vector_float_multiply.sv testbench.sv --Mdir obj_dir
# RUN: %t/generated/obj_dir/Vtestbench
