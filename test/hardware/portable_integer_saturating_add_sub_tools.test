# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-integer-saturating-add-sub-test %t
# RUN: cd %t/scalar/artifacts && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench scalar_integer_saturating_add_sub.sv testbench.sv --Mdir obj_dir
# RUN: %t/scalar/artifacts/obj_dir/Vtestbench
# RUN: cd %t/scalar/artifacts && yosys -q -s portable_scalar_integer_saturating_add_sub.ys
# RUN: cd %t/vector/artifacts && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench fixed_vector_integer_saturating_add_sub.sv testbench.sv --Mdir obj_dir
# RUN: %t/vector/artifacts/obj_dir/Vtestbench
# RUN: cd %t/vector/artifacts && yosys -q -s portable_fixed_vector_integer_saturating_add_sub.ys
