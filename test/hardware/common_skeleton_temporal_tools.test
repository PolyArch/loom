# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-common-skeleton-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module temporal_testbench temporal_module.sv temporal_testbench.sv --Mdir obj_temporal
# RUN: timeout 30s %t/obj_temporal/Vtemporal_testbench
# RUN: cd %t && yosys -q -s temporal_skeleton.ys
