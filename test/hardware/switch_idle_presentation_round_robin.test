# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-switch-idle-presentation-test %t round_robin
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module switch_idle_presentation_round_robin_testbench switch_idle_presentation_round_robin_module.sv switch_idle_presentation_round_robin_testbench.sv --Mdir obj
# RUN: timeout %loom-timeout-xlong %t/obj/Vswitch_idle_presentation_round_robin_testbench
# RUN: cd %t && yosys -q -s switch_idle_presentation_round_robin.ys
