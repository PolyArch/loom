# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-scalar-bit-reinterpret-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench scalar_bit_reinterpret_truncate.sv scalar_bit_reinterpret_extend.sv testbench.sv --Mdir obj_dir
# RUN: %t/obj_dir/Vtestbench
# RUN: cd %t && yosys -q -s portable_scalar_bit_reinterpret.ys
