# REQUIRES: circt, verilator
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-float-divide-remainder-test %t
# RUN: verilator --version | FileCheck %s --check-prefix=VERILATOR
# RUN: cd %t/generated && verilator --binary --timing --Wall --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench scalar_float_divide.sv scalar_float_remainder.sv testbench.sv --Mdir obj_dir
# RUN: %t/generated/obj_dir/Vtestbench

# VERILATOR: Verilator 5.050
