# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-scalar-integer-add-sub-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench scalar_integer_add_sub.sv testbench.sv --Mdir obj_dir
# RUN: %t/obj_dir/Vtestbench
# RUN: cd %t && yosys -q -s portable_scalar_integer_add_sub.ys
# RUN: cd %t/subtract_inactive && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench scalar_integer_add_sub.sv testbench.sv --Mdir obj_dir
# RUN: %t/subtract_inactive/obj_dir/Vtestbench
# RUN: cd %t/subtract_inactive && yosys -q -s portable_scalar_integer_add_sub.ys
