# REQUIRES: circt, verilator
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-math-erf-test %t
# RUN: cd %t/configured/generated && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench scalar_math_erf.sv testbench.sv --Mdir obj_dir
# RUN: %t/configured/generated/obj_dir/Vtestbench
