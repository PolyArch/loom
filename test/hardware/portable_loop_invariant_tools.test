# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-loop-invariant-test %t
# RUN: cd %t/artifacts && verilator --binary --timing --Wall --Wno-fatal --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench loop_invariant.sv testbench.sv --Mdir obj_dir
# RUN: %t/artifacts/obj_dir/Vtestbench
# RUN: cd %t/artifacts && yosys -q -s portable_loop_invariant.ys
# RUN: cd %t/mixed_width/artifacts && verilator --binary --timing --Wall --Wno-fatal --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench loop_invariant.sv testbench.sv --Mdir obj_dir
# RUN: %t/mixed_width/artifacts/obj_dir/Vtestbench
# RUN: cd %t/mixed_width/artifacts && yosys -q -s portable_loop_invariant.ys
# RUN: cd %t/zero_width/artifacts && verilator --binary --timing --Wall --Wno-fatal --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench loop_invariant.sv testbench.sv --Mdir obj_dir
# RUN: %t/zero_width/artifacts/obj_dir/Vtestbench
# RUN: cd %t/zero_width/artifacts && yosys -q -s portable_loop_invariant.ys
