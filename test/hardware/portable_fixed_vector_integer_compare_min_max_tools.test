# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-fixed-vector-integer-compare-min-max-test %t
# RUN: cd %t/artifacts && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench fixed_vector_integer_compare_min_max.sv testbench.sv --Mdir obj_dir
# RUN: %t/artifacts/obj_dir/Vtestbench
# RUN: cd %t/artifacts && yosys -q -s portable_fixed_vector_integer_compare_min_max.ys
