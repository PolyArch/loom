# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-scalar-float-fma-test %t
# RUN: cd %t/artifacts && verilator --binary --timing --Wall --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench scalar_float_fma.sv testbench.sv --Mdir obj_dir
# RUN: %t/artifacts/obj_dir/Vtestbench
# RUN: cd %t/singleton/artifacts && verilator --binary --timing --Wall --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench_singleton scalar_float_fma_singleton.sv testbench_singleton.sv --Mdir obj_dir
# RUN: %t/singleton/artifacts/obj_dir/Vtestbench_singleton
# RUN: cd %t/artifacts && yosys -q -s portable_scalar_float_fma.ys
