import os

config.substitutions.insert(
    0,
    (
        "%loom-hardware-implementation-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "hardware",
            "loom-hardware-implementation-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-hardware-configuration-abi-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "hardware",
            "loom-hardware-configuration-abi-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-hardware-transport-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "hardware",
            "loom-hardware-transport-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-common-skeleton-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "hardware",
            "loom-common-skeleton-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-specialization-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "hardware",
            "loom-specialization-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-circt-conformance-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "hardware",
            "loom-circt-conformance-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-hw-probe",
        os.path.join(config.loom_obj_root, "bin", "loom-hw-probe"),
    ),
)
