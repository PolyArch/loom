# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-scalar-integer-compare-min-max-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal -CFLAGS '-std=c++20' --top-module testbench scalar_integer_compare_min_max.sv testbench.sv --Mdir obj_dir
# RUN: %t/obj_dir/Vtestbench
# RUN: cd %t && yosys -q -s portable_scalar_integer_compare_min_max.ys
