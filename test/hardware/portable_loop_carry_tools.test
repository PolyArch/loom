# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-loop-carry-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench loop_carry.sv testbench.sv --Mdir obj_dir
# RUN: %t/obj_dir/Vtestbench
# RUN: cd %t && yosys -q -s portable_loop_carry.ys
# RUN: cd %t/mixed_width && verilator --binary --timing --Wall --Wno-fatal --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench loop_carry.sv testbench.sv --Mdir obj_dir
# RUN: %t/mixed_width/obj_dir/Vtestbench
# RUN: cd %t/mixed_width && yosys -q -s portable_loop_carry.ys
