# REQUIRES: circt, verilator
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-memory-hierarchy-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-DECLFILENAME --Wno-UNUSEDSIGNAL --Wno-UNSIGNED -CFLAGS '-std=c++20' --top-module testbench spatial_memory.sv spatial_memory_testbench.sv --Mdir spatial_obj
# RUN: %t/spatial_obj/Vtestbench
