# REQUIRES: circt, verilator
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-scalar-bit-reinterpret-test %t
# RUN: cd %t/provider_artifacts && verilator --binary --timing --Wall --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench scalar_bit_reinterpret_truncate.sv scalar_bit_reinterpret_extend.sv testbench.sv --Mdir obj_dir
# RUN: %t/provider_artifacts/obj_dir/Vtestbench
