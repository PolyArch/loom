# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-common-skeleton-test %t
# RUN: cd %t && verilator --binary --timing --Wall --Wno-fatal --Wno-UNUSEDSIGNAL -CFLAGS '-std=c++20' --top-module testbench loom_module.sv testbench.sv --Mdir obj_dir
# RUN: timeout %loom-timeout-xlong %t/obj_dir/Vtestbench
# RUN: cd %t && yosys -q -s common_skeleton.ys
