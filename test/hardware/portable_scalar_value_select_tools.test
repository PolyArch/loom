# REQUIRES: circt, verilator, yosys
# RUN: rm -rf %t && mkdir -p %t
# RUN: %loom-portable-scalar-value-select-test %t
# RUN: cd %t && verilator --binary --timing --Wall -CFLAGS '-std=c++20' --top-module testbench scalar_value_select.sv testbench.sv --Mdir obj_dir
# RUN: %t/obj_dir/Vtestbench
# RUN: cd %t && yosys -q -s portable_scalar_value_select.ys
