import os

config.substitutions.insert(
    0,
    (
        "%loom-common-local-reference-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-local-reference-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-common-blob-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-blob-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-common-artifact-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-artifact-test",
        ),
    ),
)
