import os

config.substitutions.insert(
    0,
    (
        "%loom-common-artifact-local-reference-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-artifact-local-reference-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-common-blob-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-blob-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-common-artifact-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-artifact-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-common-canonical-relation-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-canonical-relation-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-common-mapping-debug-log-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-mapping-debug-log-test",
        ),
    ),
)

config.substitutions.insert(
    0,
    (
        "%loom-common-special-math-accuracy-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-special-math-accuracy-test",
        ),
    ),
)
