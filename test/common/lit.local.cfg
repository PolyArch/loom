import os

config.substitutions.insert(
    0,
    (
        "%loom-common-artifact-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "common",
            "loom-common-artifact-test",
        ),
    ),
)
