import os

config.substitutions.insert(
    0,
    (
        "%loom-implementation-platform-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "implementation_platform",
            "loom-implementation-platform-test",
        ),
    ),
)
