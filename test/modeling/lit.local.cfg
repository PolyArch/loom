import os

config.substitutions.insert(
    0,
    (
        "%loom-fast-fpa-model-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "modeling",
            "loom-fast-fpa-model-test",
        ),
    ),
)
