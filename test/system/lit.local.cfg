import os

config.parallelism_group = "resource-intensive"

config.substitutions.insert(
    0,
    (
        "%loom-execution-matrix-lifecycle-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "system",
            "loom-execution-matrix-lifecycle-test",
        ),
    ),
)
