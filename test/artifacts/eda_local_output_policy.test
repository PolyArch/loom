# RUN: cd %S/../.. && git check-ignore -q build/.loom-policy-probe
# RUN: cd %S/../.. && git check-ignore -q loom-local-config.json

# Repository-local generated output and machine-local configuration must stay ignored.
