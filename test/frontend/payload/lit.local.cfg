import os

config.suffixes = [".test"]

config.substitutions.insert(
    0,
    (
        "%loom-frontend-payload-framing-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "frontend",
            "payload",
            "loom-frontend-payload-framing-test",
        ),
    ),
)
config.substitutions.insert(
    0,
    (
        "%loom-relocatable-payload-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "frontend",
            "payload",
            "loom-relocatable-payload-test",
        ),
    ),
)
