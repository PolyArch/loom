import os

config.substitutions.insert(
    0,
    (
        "%loom-evaluation-metric-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "evaluation",
            "loom-evaluation-metric-test",
        ),
    ),
)
