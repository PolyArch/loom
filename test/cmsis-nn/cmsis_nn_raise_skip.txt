# Per-corpus raise-pipeline skip list for run_cmsis_nn_raise.sh.
#
# Format:
#   <src_relpath> # <one-line reason>
#
# - src_relpath matches the first column of cmsis_nn_targets.txt.
# - The inline `# reason` portion is mandatory in spirit so the next
#   reader can audit why a row is masked. The runner does not enforce
#   non-empty reasons, but please add one so the audit trail survives.
# - Blank lines and lines beginning with `#` are ignored.
#
# Skip budget: 5 entries. The runner exits with status 3 if more than
# 5 rows are listed here, on the theory that beyond that threshold we
# are masking a systemic loom-raise regression that should be fixed
# in lib/Frontend/Raising rather than papered over here.
#
# Skipped rows are still exercised by the IR-only runner
# (run_cmsis_nn_ir.sh). Only the raise pass is bypassed.
#
# As of this commit, no cmsis-nn source needs skipping: all 18 rows
# raise cleanly, 14 with structured-control ops recovered.
