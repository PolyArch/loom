import os

config.substitutions.insert(
    0,
    (
        "%loom-pnr-index-test",
        os.path.join(
            config.loom_obj_root, "test", "pnr", "loom-pnr-index-test"
        ),
    ),
)
config.substitutions.insert(
    0,
    (
        "%loom-pnr-config-test",
        os.path.join(config.loom_obj_root, "test", "pnr", "loom-pnr-config-test"),
    ),
)
config.substitutions.insert(
    0,
    (
        "%loom-pnr-route-tree-state-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "pnr",
            "loom-pnr-route-tree-state-test",
        ),
    ),
)
config.substitutions.insert(
    0,
    (
        "%loom-pnr-routing-negotiation-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "pnr",
            "loom-pnr-routing-negotiation-test",
        ),
    ),
)
