import os

config.substitutions.insert(
    0,
    (
        "%loom-application-manifest-test",
        os.path.join(
            config.loom_obj_root,
            "test",
            "applications",
            "loom-application-manifest-test",
        ),
    ),
)
