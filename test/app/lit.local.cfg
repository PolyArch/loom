# test/app/ is a smoke-test corpus driven by run_all.sh and
# run_raise_all.sh -- not a lit test source tree. Each kernel sub-dir
# has a `build/` cache that may contain `.scf.mlir` artifacts emitted by
# raise_check.sh; we don't want lit to discover those as test files.
config.suffixes = []
