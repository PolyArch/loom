# Review 0820-0825 closure ledger (G3), created 2026-09-02 before G3 start

Each row: semantic owner -> production call path -> focused evidence -> independent replay -> performance data -> exact status.
Status vocabulary: open | in-progress | closed | typed-incomplete(<reason>) | deferred-performance(<baseline>) | deferred-spec(<anchor>).

| Row | Item (from plan section 7) | Owner | Production call path | Focused evidence | Independent replay | Performance data | Status |
|---|---|---|---|---|---|---|---|
| E0 | Current-WIP regression replay: every 0823/0824 item that 0827 marked closed and whose owner is touched by the WIP (schema, Fabric handshake, Mapping artifact, PnR objective/config, |  |  |  |  |  | open |
| E1 | Wire `loom-structured-ownership-evaluation-test` into `LOOM_TEST_DEPENDS` and lit. |  |  |  |  |  | open |
| E2 | Derive `test/simulation_conformance.py:_FABRIC_SCHEMA` from the codec owner (7.1). |  |  |  |  |  | open |
| E3 | Tracked ten-profile CGRA simulation gate input with its generator (`qualify_cgra_budget.py`) owned by build/lit. |  |  |  |  |  | open |
| E4 | Decide and implement the `FabricArtifactBytecode` producer string `loom.fabric.3.0` in canonical identity bytes. |  |  |  |  |  | open |
| E5 | Reader for `unsupportedMemoryContract` evidence or removal of the field. |  |  |  |  |  | open |
| E6 | Spec/rationale synchronization for every schema bump in the G2 commits and the twelve documents named by 0822-0. One-run evidence items: |  |  |  |  |  | open |
| E7 | FPA finalist join: Attention + PageRank rows with `--loom-fpa-weight-root`, asserting `hardware_promotion_observations[].promoted_to_exact_mapping`. |  |  |  |  |  | open |
| E8 | Real OpenROAD leaf campaign (offline, four-hour budget, weight <= 10 GB) with `/mnt/nas0/software/openroad/2026.08.25-*` and gpdk045 through `buildFpaGroundTruthCollectionPlan`, or |  |  |  |  |  | open |
| E9 | Product spectrum artifact through `verify_product_visualization.py --expected-spectrum-class --require-transition --minimum-region-count` on context-24 Attention plus one non-Atten |  |  |  |  |  | open |
| E10 | Evidence manifest regeneration from a clean checkout, join status restated against the owner-scoped status plus run-key digest. |  |  |  |  |  | open |
| E11 | Full-suite evidence publication in a tracked location or checkpoint note. Implementation items (time-boxed, parallel on disjoint owners): |  |  |  |  |  | open |
| E12 | Live-state migration domain: typed state-correspondence records, live-state classes, nonzero migration/reprogramming cost accounting, application replay with one nonempty live-stat |  |  |  |  |  | open |
| E13 | Synchronous safe-point drive: a production caller for `prepareGem5SystemCompletionControlledInvocation`. |  |  |  |  |  | open |
| E14 | Dedicated `SpatialTopology`, `InstructionCapacity`, `SystemInstructionContext` mutation families with durable per-family `FinalizedDeployment`/`ResourceTimeTransition` records. |  |  |  |  |  | open |
| E15 | DynamicWork production join: frontend emission, stable `WorkItemId` projection, application caller, deterministic replay, typed refusal. |  |  |  |  |  | open |
| E16 | CGRA atomics/volatile/fence fallback decision path with one verified fallback replay per admitted contract class. |  |  |  |  |  | open |
| E17 | Ordered-channel System-level lifecycle (generations/epochs, bounded host LLJIT capacity, cancellation, negative rate case) under one application identity. |  |  |  |  |  | open |
| E18 | Polyhedral tiling/strip-mining/parallel/reduction proposal production and a polyhedral-to-System replay artifact. |  |  |  |  |  | open |
| E19 | Funnel exact-comparison sample against real PnR with `OutOfDistribution` assignment. |  |  |  |  |  | open |
| E20 | Portfolio joined verdict (`evaluate_portfolio` wired), full QoR dimensions (six of eleven null), TinyML product profile beyond 0-warmup/1-measured, workload/runtime-input identity  |  |  |  |  |  | open |
| E21 | gem5 System-simulation host-side performance (0825 analysis, complete contract): per-phase wall / self CPU / child CPU for buildGem5Binding, prepare, external execution, import, Ev |  |  |  |  |  | open |
| E22 | Responsibility decisions per file above 2,000 lines (thirteen production, six test files at the branch head), the audit-named owners, and the `loom-system-run.cpp` mapped-RTL extraction. | `tools/loom-system-run` (extraction executed: `MappedRtlCell.{h,cpp}`, `SpatialInvocationCase.h`); decisions per file in appendix E22 below with their owners; `include/PnR/PhysicalTagKeyedMap.h` is the one tag-keyed flat map now shared by the tag assignment occupancy and the progress state | `loom-system-run --mapped-rtl` (`deriveMappedRtlDeployment`, `executeMappedRtlCell`, then the driver's `completeSpatialRun`) | commit `289d8d24d`; `build/test/system/product_matmul_checksum_execution.test` PASS on the rewired driver (value 4C3 = 1219 from system DFG/CGRA and Spatial DFG/CGRA cells); appendix E22 | `llvm-lit build/test/system/product_matmul_checksum_execution.test` (gem5 readiness linked from the primary build); mapped-RTL cell `product_matmul_checksum_mapped_rtl_execution.test` | driver 2076 -> 1847 lines; product cell 3:39 wall at load 80-100 (reference 35 s quiet); mapped-RTL cell not replayed under the slice-window load | closed for the mandated extraction and the per-file decisions; typed-incomplete(other extractions are decisions with owners, not executed: MappingProgressAnalysis and SpatialActionExecutor/PathFinder splits follow C2, T1.2, and nego-1 in the PnR owner; `test/dse/RootCompleteSpatialPnrGeneratorTest.cpp` crossed 2,000 lines by the routing-interruption scenario of this slice and its split seam is recorded below) |
| E23 | PnR deep-review residual rows (T0.3, T1.1-T1.5, A.3, A.7, A.8, tag-1/2, recur-1, noop-1, ripup-1, domain-1, snap-1, astar-1, nego-1/2, init-1, freeze-1/2, sys-cap-3, sys-probe-1, sys-route-4, sys-domain-8, tech-2, tech-5..8, D6, tech-1-safe, verify-1) | per row in appendix E23 (lib/PnR, lib/PnR/System, lib/PnR/CpSat, lib/Mapping/Tech, lib/Fabric/Identity) | `loom-c++` Mapping execution -> `generateSpatialMappings` -> annealing -> executor -> PathFinder -> EndpointRouter; System PnR generator; `loom-cgra-budget-profile` drives the same chain with the collector policy | `temp/pnr-perf-owners/c3/quiet-{off-branch,on-branch,on-main}-depth16-vc-1/` (quiet-host summary diagnostics, GNU time, load), `c3/perf-off-branch-depth16-vc-1/perf-top.txt` (flat symbol ranking), `c3/callgraph-tag-keyed-map-depth16-vc-1/perf-{children,callgraph}.txt` (inclusive ranking and caller chains of a 75 s window), `c3/ab{1,2}-*` (paired lever A/B), `temp/pnr-perf-owners/c1/cegar-1/` (CEGAR iteration records); code anchors per row | `temp/pnr-perf-owners/run_profile_case.sh <build-or-binary> <out> 16 per_tag_virtual_channel 560 <unit>`; `run_perf_case.sh` and `run_perf_callgraph_case.sh` for the rankings; `python3 temp/pnr-perf-owners/compare_traces.py A/stderr.txt B/stderr.txt`; `llvm-lit build/test/mapping/tech_mapping_search.test` (A.3) | quiet host: depth-16 VC exhaustive profile annealing max 127.3 s assertion-off and 139.4 s assertion-on, 64/64 levels, traces equal; inclusive window shares: probeBatch 60.5 % (close 35.6 %, route 13.8 %, tag staging 12.0 %, capacity projection 6.4 %), resolve/commit 10.8 %, allocator about 16 % self, freeze 16 % of the window (handshake projection index 13 %); CEGAR per iteration: freeze 4.4-5.6 s CPU, exact repair 0.4-0.8 s, runtime evaluation 0.3-0.4 s, Evidence verification 0.007-0.2 s | closed: A.3, nego-1 (subset), snap-1; deferred-spec: T1.1, A.7, A.8, tag-1 handshake row identity; deferred-performance (baseline and next lever recorded, no section 2 gate affected): recur-1, noop-1, ripup-1, domain-1, astar-1, nego-2, init-1, tech-2/5-8/1-safe, D6; typed-incomplete(80 s exhaustive target unmet; two allocation levers landed with trace equality, wall effect pending a quiet host): T0.3, T1.2/tag-2, T1.3/freeze-1/2, T1.4, T1.5/sys-*, verify-1 |
| C1 | CEGAR cost structure on the StrictFifo negative path; cooperative deadline into the PathFinder/EndpointRouter inner loop | `lib/DSE/Feedback/SpatialTransportCegar.cpp` (loop), `lib/PnR/SpatialPathFinderRouter.cpp` (`routeToClosureInMove` control boundaries), executor/annealing/repair/seed/closure classification; Fabric-only derived contexts reused through the application's `PnrDerivedContextSession` (`lib/Application/MappingExecution.cpp`) | `loom-c++ --loom-accel-profile=case4-depth16-strictfifo-collector-pnr.yaml --loom-mapping-repair-candidate-limit=8` -> `executeSpatialTransportRuntimeRepair` -> `executeSpatialTransportCegar`; every Spatial route through `routeToClosureInMove` | commit `96554130d`; `temp/pnr-perf-owners/c1/cegar-1/` (3 iterations, retired at iteration 2, child 944 CGRA cycles, pair `verified_feasible_but_not_beneficial`, identical to the 09-01 record; session statistics: 12 requests, 7 hits, 5 misses, 3.87 s construction saved); `test/dse/root_complete_spatial_pnr_interruption.test` (43/43 focused cells); canonical trace equality against main: quiet-host assertion-on main versus branch 54 common levels, 0 differing fields (`c3/quiet-on-main-depth16-vc-1` versus `c3/quiet-on-branch-depth16-vc-1`; main was tier-limited by a load spike) and 23 common levels earlier | `temp/pnr-perf-owners/run_cegar_build.sh <build> <out> temp/deadlock-closure-0901/a2/profiles/case4-depth16-strictfifo-collector-pnr.yaml 3600000 <unit> --loom-mapping-repair-candidate-limit=8`; `llvm-lit build/test/dse/root_complete_spatial_pnr_interruption.test` | per CEGAR iteration (CPU): freeze 4.4-5.6 s (65-82 %), exact repair 0.38-0.83 s, runtime evaluation 0.30-0.38 s, Evidence verification 0.007-0.19 s, promotion 0.23-0.55 s, warm seed 0.33-0.38 s; Mapping repair 24.2 s wall; loom-c++ 24:46 wall / 2202 s CPU / 5.4 GB at load 119-34; deadline overrun 1.59 s (assertion-on, load 35), 7.26 s (assertion-off, load 130), 0 on the quiet host (both builds, 64/64 levels) | deadline propagation closed; typed-incomplete(the 43 s Evidence replay premise no longer holds: replay is 0.007-0.2 s per iteration and the Fabric-only contexts already hit the session; the dominant cost is the per-iteration workload freeze, whose profile charges the frozen handshake projection index (`FrozenSpatialHandshakeIndexBuilder::BuildState::buildDenseProjectionIndex`, string-pair-keyed maps, 13 % of the sampled window) first; next lever: typed ordinal keys in that index, then a refreeze that reuses the constraint-independent workload indexes) |
| C2 | depth1/depth2 control cells: typed static capacity verdicts instead of exhausting the fast tier | `lib/Mapping/Artifact/MappingProgressAnalysis.cpp` (`deriveMappingReconvergentCapacityProof`: pool >= distinct selected nets per FIFO, Mapping-dependent), `lib/PnR/SpatialProgressState.cpp`, Spatial generator infeasibility proofs | `loom-cgra-budget-profile ... 1 strict_fifo` / `1 per_tag_virtual_channel` / `2 per_tag_virtual_channel` | the 09-01 cells (`temp/deadlock-closure-0901/a3/case{1,2,3}-*`) and the capacity proof owner read in this slice: the proven obligation is `pool >= |selected nets through the FIFO|`, so at depth 1 or 2 a Mapping that spreads nets over the 1080 FIFO occurrences (119 nets) satisfies every proven obligation and no Mapping-independent shortfall exists in the current proof | `temp/pnr-perf-owners/run_profile_case.sh <build> <out> 1 per_tag_virtual_channel 560 <unit>` (and `2 per_tag_virtual_channel`, `1 strict_fifo`) | 09-01 cells: 180.6 s wall, 692-703 s CPU, 5.6 GB, `candidate_cancelled_or_timeout`, 0 publications; this slice, depth-1 VC on the branch assertion-off build at load 55 (`temp/pnr-perf-owners/c2/off-depth1-vc-1`): 181.5 s wall, 569 s CPU, overrun 1.12 s, 0 publications, and at every completed level the only nonzero violation kind is `HardProgressViolation` (144 at level 0, 67 at level 4 of the best incumbent; unrouted, capacity-overuse, tag, proof-debt, and handshake kinds are 0 from level 2), so the fast tier is spent reducing closed-wait witnesses, not capacity shortfalls; depth-2 VC (`c2/off-depth2-vc-1`): 181.4 s wall, 381 s CPU, overrun 1.16 s, 0 publications, best residual `HardProgressViolation` 56 with every other kind 0 after level 1 | typed-incomplete(no Mapping-independent static criterion exists: the reconvergent capacity proof is `pool >= |selected nets per FIFO|` and closes at depth 1 for any Mapping that spreads 119 nets over 1080 FIFO occurrences, and the remaining family is `HardProgressViolation`; next action: a Mapping-independent lower bound on closed-wait witnesses in `deriveMappingDataflowProgressBasis` from the nets that the fabric topology forces through one FIFO owner at the recipe depth, returned as `ProvenInfeasibleSpatialMapping` with `SpatialPnrInfeasibilityProofKind::FrozenActiveProblem` before restarts; when that bound is zero the fast tier legitimately remains the runtime oracle) |
| C3 | Exhaustive Spatial PnR diagnostic profile toward 80 s with assertion-on/off paired A/B and canonical trace equality | lib/PnR annealing/executor/router/tag/progress owners (0828-1 lever list plus the call-graph ranking of this slice); driver `test/system/CgraBudgetProfile.cpp` (reports `deadline_ns`, `deadline_overrun_ns`) | `loom-cgra-budget-profile <store> <report> matmul loombench:matmul:a3bfb46cf514cea5 main 16 per_tag_virtual_channel` | quiet host (load 1.2-4): `c3/quiet-off-branch-depth16-vc-1` (branch, assertion-off), `c3/quiet-on-branch-depth16-vc-1` (branch, assertion-on), `c3/quiet-on-main-depth16-vc-1` (main, assertion-on, load spike to 22 during the run); levers: commits `f35cb0d86` (tag-keyed queue-class refcounts, scan diagnostics) and `74b9e40dd` (flat capacity projection) with paired A/B `c3/ab1-{base,lever}-depth16-vc-1` and `c3/ab2-{base,lever}-depth16-vc-1`; rankings `c3/perf-off-branch-depth16-vc-1/perf-top.txt`, `c3/callgraph-tag-keyed-map-depth16-vc-1/perf-children.txt`; preserved binaries under `temp/pnr-perf-owners/bin/` | `temp/pnr-perf-owners/run_profile_case.sh` on the three builds or preserved binaries plus `compare_traces.py`; per-restart metric `spatial_restart_report.annealing_ns` via `canonical_trace.py metrics` | quiet host, branch assertion-off: annealing max 127.3 s (restarts 122.2 / 108.9 / 127.3 / 123.2 s), 64/64 levels, `spatial_pnr` phase 142.3 s wall / 528 s CPU, cell 158.9 s wall / 542.6 s CPU / 5.56 GB, overrun 0; branch assertion-on: 139.4 s max, 64/64, phase 156.7 s, cell 176.1 s / 600.6 s CPU; on/off trace equality 64 common levels, 0 differing fields; main assertion-on (loaded): 164.8 s max, 54 common levels, 0 differing fields against both branch runs; references: 0828-1 assertion-off 132.3-133.0 s, 09-01 assertion-on 156-174 s; paired lever A/B under load 29-33 (tier-limited): ab1 base 161.8 s / 43 levels versus lever 161.7 s / 46 levels, 43 common levels equal; ab2 base 151.0 s / 37 levels versus lever 154.1 s / 39 levels, 36 common levels equal; per-level diagnostics: 262,526 probes of which 97,999 semantic no-ops, 514,762 arc-cost validation scans over 46 levels (4.97 per accepted action, 1.27 per heuristic lookup) | deferred-performance(baseline 127.3 s assertion-off on the quiet host against the 80 s target, no section 2 gate affected; two allocation levers landed with trace equality, their wall effect typed-incomplete(host load 22-33 kept every paired run inside the 180 s tier) until a quiet-host replay of `bin/loom-cgra-budget-profile.scan-diagnostics` versus the branch head; next levers in order: owner-certified arc cost validation in `SpatialRouteCostState` so `EndpointRouteSearchScratch::search` stops scanning every arc per query, demand-vector reuse in `SpatialRouteCostState::commitTagProjectionDelta`, then the 0828-1 handshake row identity which is deferred-spec) |

### E22 responsibility decisions (per file; line counts at branch g3/pnr-perf-owners)

Executed in this slice:
- `tools/loom-system-run/loom-system-run.cpp` 2076 -> 1847: the mapped-RTL responsibility (deployment rebuild as portable Spatial-core RTL with runtime platform bindings, Verilator provider binding, Request publication, frozen bundle execution, Evidence import) is `tools/loom-system-run/MappedRtlCell.{h,cpp}` (`loom::system_run::deriveMappedRtlDeployment`, `executeMappedRtlCell`, typed `MappedRtlProviderOptions`, `MappedRtlCellEvidence`); the invocation case observed by both System engines is `SpatialInvocationCase.h`. The driver keeps engine-common completion (`completeSpatialRun`), comparison, manifest, and the CLI options it projects into the provider options. Next seam when it grows again: `writeManifest` plus the Spatial CGRA profile writer into a manifest module.

Decisions without extraction in this slice (owner, seam, cohesion argument):
- `lib/DSE/Generators/Compiler/PreMappingExploration.cpp` 2863: two owners share the file: ownership-candidate exploration (obligation canonicalization, quality gate, `exploreOwnershipCandidates`) and the pre-Mapping stage driver `exploreStructuredCompilationToPreMapping` (about 1590 lines in one function). Decision: extract the ownership selection stage into `PreMappingOwnershipSelection.cpp` behind `exploreOwnershipCandidates` and its result types; the stage driver then stays under 1300 lines. Owner: compiler generator (frontend slice); not executed here because the file is outside this slice's owner set and the E18 slice is editing the same generator family.
- `lib/Mapping/Artifact/MappingProgressAnalysis.cpp` 2849 (this slice's owner): three responsibilities: the System event dependency graph and resource-time progress qualification (`SystemEventDependencyGraph`, `buildSystemEventDependencies`, `qualifySystemMappingResourceTimeProgress`), the actor dependency graph with the buffered-dependency closure (`ActorDependencyGraph`, `mappingCompletionFrontierIsAdmissible`, the `MappingProgressClosure` derivations), and the Spatial channel storage inventory with the reconvergent capacity proof (`projectSpatialChannelStorageInventory`, `deriveMappingReconvergentCapacityProof`). Decision: extract the channel storage inventory and capacity proof (about 780 lines) into `SpatialChannelStorageProgress.cpp` and the System event dependency family (about 400 lines) into `SystemEventProgress.cpp`, each behind the already public declarations in `MappingProgressAnalysis.h`; the shared byte-key helpers move to an internal header. Deferred behind C2: the C2 static capacity verdict lands in the channel storage owner, and splitting the file before that semantic change would move the same lines twice.
- `lib/PnR/SpatialActionExecutor.cpp` 2384 (this slice's owner, touched by C1): the explicit logical-memory binding reconciliation family (`recordExplicitLogicalMemoryBinding` through `reconcileBindingRelations`, about 640 lines) is a coherent responsibility separate from dependency closure and route staging. Decision: second translation unit `SpatialActionMemoryReconciliation.cpp` of the same scratch class with an internal header for the executor error helpers. Not executed in this slice: the C1 change touches the routing classification only, and the extraction needs its own A/B replay on the register-FIFO fixtures (PageRank three-pairing cells) before it is behavior-proven.
- `lib/PnR/CpSat/SpatialExactRepair.cpp` 2359: one function, `repairTransportClosureRegion` (about 1470 lines), is the size driver; a file split would not separate responsibilities. Decision: decompose the region repair loop into typed phases (region model build, solve, replay probe, certificate consumption) inside the CpSat owner; keep one file until that decomposition exists. Cohesion argument: the loop shares one region state (decisions, nets, cut certificates) across every phase.
- `lib/PnR/SpatialCandidateInitializer.cpp` 2081: `preferScheduleAwareRootPlacements` (about 1160 lines) derives the root placement preference and is a pure function of the frozen problem; `SpatialInitializerAttemptBuilder` constructs the attempt. Decision: extract the preference derivation into `SpatialInitializerRootPreference.cpp`. Owner: PnR initializer; next slice, because `init-1` (hop-distance memoization) lands in the same function and the split should follow that change.
- `lib/PnR/SpatialTagAssignment.cpp` 2080: cold canonical derivation (`deriveCanonicalSpatialTagAssignments`, storage build, coloring, relation verification) versus the incremental `SpatialTagAssignmentState` mutation API. Decision: extract the cold derivation into `SpatialTagAssignmentDerivation.cpp` once T1.2's delta projection reshapes the mutation API; splitting before T1.2 would fix the wrong boundary.
- `lib/PnR/SpatialPathFinderRouter.cpp` 2067 (touched by C1): `analyzeCapacityConflicts` and the fixed-terminal cut certificate derivation (about 515 lines, the `cut*` member state) are separable from the negotiation loop `routeToClosureInMove`. Decision: second translation unit `SpatialPathFinderConflictAnalysis.cpp`. Not executed here: the C1 edit adds two control observations to the loop and the conflict analysis is the `nego-1` lever's owner, so the split follows that lever.
- `lib/Hardware/RTL/Hierarchy/Memory.cpp` 2139: `buildMemoryModule` (about 1170 lines) elaborates one hardware module; the helper decomposition (row decode, request mux, address projection, service byte addressing) already exists. Decision: keep; cohesion argument: one module generator whose remaining monolith is the sequential elaboration order of ports and state. Next seam if it grows: the local-service access path (`deriveLocalServiceAccessCases`, `projectLocalRequest`, `serviceAddressByte`) into `MemoryServiceAccess.cpp`. Owner: RTL hierarchy (D4 owner), outside this slice.
- `lib/DSE/JointHardwareReopen.cpp` 2091: `executeJointDesignWithHardwareReopen` (about 1257 lines) plus the hardware feedback reopen family (`exploreFinalizedMappingHardwareSpectrum`, `tryHardwareFeedbackReopen`, about 760 lines). Decision: extract the feedback reopen family into `JointHardwareFeedbackReopen.cpp`; the joint driver keeps the pair loop. Owner: DSE joint (spectrum slice E12-E17), not executed here.
- `lib/ExternalTool/InvocationBundle.cpp` 2072: manifest JSON codec (parse and write, about 800 lines) versus bundle staging, file descriptors, and execution. Decision: extract the codec into `InvocationManifestCodec.cpp`. Owner: ExternalTool (integration slice E21), not executed here.
- `lib/Fabric/Identity/FabricRefImport.cpp` 2043: `FabricArtifactView` accessor implementations over one immutable decoded storage. Decision: keep; cohesion argument: the size is accessor breadth of one view type, not tangled responsibilities; every accessor reads the same storage and none carries logic beyond lookup. Next seam if it grows: the transport endpoint and tag accessor family into `FabricArtifactViewTransport.cpp`.
- `lib/Fabric/Artifact/FabricArtifact.cpp` 2030: strict import, System view build (`buildSystemView`, service capability validation), and finalize/publish. Decision: extract the System view build with its capability validation (about 690 lines) into `FabricSystemView.cpp`. Owner: Fabric artifact, outside this slice.

Test files above 2000 lines:
- `test/pnr/SystemCandidateStateTest.cpp` 2780: two workflows (`memoryServiceWorkflow`, `graphBindingWorkflow`) already share `SystemCandidateStateTestSupport.{h,cpp}` and `SystemNegotiatedRoutingWorkflowTestSupport.cpp`. Decision: split the memory-service workflow into its own executable (`SystemCandidateMemoryServiceTest.cpp`) on the existing support; the lit cells select the workflow by name so the split is mechanical. Not executed here (evidence file of the System PnR owner; a split must keep both lit cells green).
- `test/frontend/StructuredOwnershipEvaluationTest.cpp` 2123: one evaluation anchor with four scenarios; the fixture builders dominate. Decision: keep; E1 (wiring into lit) is the open item, not the size.
- `test/dse/JointDesignExplorationTest.cpp` 2099: one binary with mode dispatch across mutation families and feedback/quality/witness modes. Decision: split the mutation-family modes into `JointDesignMutationTest.cpp`; owner DSE, not executed here.
- `test/hardware/CommonSkeletonTest.cpp` 2008 and `test/external_tool/InvocationBundleTest.cpp` 2005: single-subject evidence files just over the threshold. Decision: keep; the next scenario added to either goes into a sibling file.
- `test/dse/RootCompleteSpatialPnrGeneratorTest.cpp` 2083 (crossed by the 85-line routing-interruption scenario of commit `96554130d`): two scenario families share one mode dispatch, the Spatial PnR generator scenarios (`empty-constraint-owner` through `foreign-fabric-rejection`) and the Spatial Mapping promotion/feedback scenarios (`spatial-mapping-promotion`, `spatial-mapping-lineage`, `spatial-mapping-feedback`, `spatial-mapping-feedback-replay`, about 730 lines, lit cells `root_complete_spatial_pnr_{promotion,lineage,feedback,feedback_replay}.test`). Decision: move the four promotion/feedback scenarios into `RootCompleteSpatialMappingFeedbackTest.cpp` as a second executable on the existing `RootCompleteSpatialPnrTestSupport` and `RootCompleteSpatialFeedbackTestSupport` units, after lifting the file-local fixture builders (`buildFixture`, `generateTechMapping*`, `generateSpatialMapping*`, `publish*SpatialInputs`) into the support library; the four lit cells and `test/dse/lit.local.cfg` gain the second substitution. Not executed in this slice: the split is mechanical but must keep the twelve generator cells and the four feedback cells green, which needs a quiet host for the promotion cells.

Audit-named owners below the threshold:
- `lib/Application/Build.cpp` 1070: product build driver; keep.
- `lib/Application/BuildDiagnostics.cpp` 1975: build-stage diagnostics emission; keep, with the next family of diagnostics going into a sibling file (it is 25 lines under the threshold).
- `lib/Mapping/Artifact/SpatialMappingArtifact.cpp` 1941: SpatialMapping codec plus verification; the verification family is the extraction candidate when the file crosses 2000.

### E23 sub-ledger (PnR deep-review residual rows)

Production call path shared by every Spatial row unless stated: `loom-c++` (`lib/Application/MappingExecution.cpp`) -> `RootCompleteSpatialPnrCandidateGenerator` -> `pnr::generateSpatialMappings` (`lib/PnR/Generator/SpatialPnrGenerator.cpp`, `runSpatialRestartImpl`) -> `createPathFinderSpatialSeed` -> `SpatialAnnealingSearchScratch::run` -> `SpatialActionExecutorScratch::probe` -> `SpatialPathFinderRouterScratch::routeToClosureInMove` -> `SpatialNetRouterScratch::routeWholeNet` / `routeSinkSet` -> `EndpointRouteSearchScratch::search`; the same chain is what `loom-cgra-budget-profile` drives through `invokeMappedBuiltinSpatialPnrFixture` with the collector's `exhaust_configured_work` policy (four restarts, master seed 0). Evidence artifact for the performance rows: `temp/pnr-perf-owners/c3/<run>/{stdout.json,stderr.txt,time.txt,perf-top.txt}` (summary diagnostics at `LOOM_VERBOSE_LEVEL=1`, GNU time, perf ranking), produced by `temp/pnr-perf-owners/run_profile_case.sh` and `run_perf_case.sh`; canonical trace projection `temp/pnr-perf-owners/canonical_trace.py`. Replay: `temp/pnr-perf-owners/run_profile_case.sh <build> <out> 16 per_tag_virtual_channel 900 <unit>`.

| Row | Owner (semantic anchor) | Evidence on this branch | Typed residual / status |
|---|---|---|---|
| T0.3 | `lib/PnR/EndpointRouter.cpp` `EndpointRouteSearchScratch::search` (arc-indexed timing memo, bucket LIFO, digest index) | perf ranking share of the endpoint search cluster in the depth-16 run (`perf-top.txt`); `annealing_level.heuristic_cache_*` and `forward_heuristic_*` fields per level | typed-incomplete(traversal-indexed timing memo needs the typed invariant that every arc of one traversal carries one timing value; the generic request promises per-arc arrays) |
| T1.1 | `EndpointRouter.cpp` forward heuristic (`forwardHeuristicQueryCount`, `forwardHeuristicUnreachableCount`) | `annealing_level.forward_heuristic_unreachable_ratio` per level in the run (about 0.09 on the depth-16 control) | deferred-spec(docs/spec-pnr-provider-builtin.md "PathFinder Routing" and the Spatial config descriptor: a resumable reverse frontier or split heuristic changes charged endpoint work and needs a descriptor bump plus corpus A/B) |
| T1.2, tag-1, tag-2 | `lib/PnR/SpatialTagAssignment.cpp` (`SpatialTagAssignmentState::stageRouteUpdates`: routed nets, changed domains, synchronized nets, `deriveIndependentColoring` with the coloring cache and recomputed identities), `SpatialActionExecutorScratch::synchronizeCandidateTags`, `SpatialRouteCostState::synchronizeTagProjection` (route-changed nets only since 134f8b038), `deriveSpatialTemporalSwitchHandshakeDomainFragments` (per changed domain), `stageSpatialTagInterferenceUpdate` (full `rebuildCanonicalInventory` and `rebuildGlobal` per staged update) | inclusive window shares: `stageRouteUpdates` 12.0 %, `synchronizeTagProjection` 8.2 % (`synchronizeCandidateSwitchRows` 7.2 %), `deriveSpatialTemporalSwitchHandshakeDomainFragments` 3.3 % flat, `rebuildGlobal` 1.5 % plus conflict-pair iteration 1.2 %, `rebuildTagIncidenceIndexes` 2.2 % flat (`c3/perf-off-branch-depth16-vc-1/perf-top.txt`, `c3/callgraph-tag-keyed-map-depth16-vc-1/perf-children.txt`); `annealing_level.accepted_actions` per level | tag-1 deferred-spec(docs/spec-fabric-module.md "Every Temporal switch (occurrence, resident row, input) retains distinct local junction identity" and docs/spec-pnr.md "materializes a graph only from its currently selected fragments" bind the candidate fragment to the tag-ordered physical row of `projectFabricTemporalSwitchCandidateRouteRowMemberSpans`, so a recolor shifts every later row's fragment identity; a row-permutation-invariant candidate projection needs a spec amendment before code); tag-2/T1.2 typed-incomplete(the delta-scope projection exists; the remaining full rebuilds are the interference inventory/global pair set per staged update and the whole-candidate independent coloring, both with the inverse journals already in place) |
| T1.3, freeze-1, freeze-2 | `lib/PnR/SpatialPnrProblem.cpp` `FrozenSpatialPnrProblemBuilder::build`; `lib/PnR/SpatialPnrHandshakeIndex.cpp` (`FrozenSpatialHandshakeIndexBuilder::BuildState::buildDenseProjectionIndex`); `lib/PnR/PnrDerivedContextSession.cpp`; `lib/DSE/Feedback/SpatialTransportCegar.cpp` (per-iteration `freezeSpatialPnrProblem`); `lib/DSE/Feedback/SpatialMappingFeedbackCandidateGenerator.cpp` (own freeze call) | CEGAR per-iteration `problem_freeze` wall in `spatial_transport_cegar_iteration` records (`temp/pnr-perf-owners/c1/cegar-1/build.stderr`); derived-context session statistics (7 hits / 5 misses, 3.87 s saved); call-graph window: `FrozenSpatialPnrProblemBuilder::build` 16.0 % with `buildDenseProjectionIndex` 13.0 % and its `std::map<std::pair<std::string, std::string>, ...>` / `std::map<std::string, std::optional<...>>` keys behind the `memcmp` share (`c3/callgraph-tag-keyed-map-depth16-vc-1/perf-callgraph.txt`) | typed-incomplete(the Fabric-only contexts are session-cached; the per-TechMapping freeze is dominated by the handshake projection index built over canonical byte keys; next lever: typed ordinal keys for that index, then ref-to-ordinal maps, by-value fragment selectors, and the feedback generator's second freeze) |
| T1.4 | `lib/Fabric/Identity/FabricHandshake*.cpp`, `FabricArtifactImportSession` (`lib/Fabric/Artifact/FabricArtifact.cpp`), `resolveSelectedHandshakeGraph` | `emitFabricDerivedContextStatistics` static/timing context hits and misses in the run | typed-incomplete(retain `FabricHandshakeContext` in the import-session entry and move the owner lookup into the context; `revalidateFabricHandshakeContext` stays at use sites) |
| T1.5, sys-route-4, sys-cap-3, sys-probe-1, sys-domain-8 | `lib/PnR/System/SystemServiceRouter.cpp` `buildSystemServiceRoutes`; `SystemCapacityProjection.cpp` `finalizeSystemCandidateProjection`; `SystemPnrSearchDomain.cpp` `SystemActionDomainScratch::rebuild`; `SystemPnrGenerator.cpp` | System PnR wall inside the product Matmul build (`mapping_execution` operation and `system_pnr` invocation statistics in the CEGAR run's `build.stderr`) | typed-incomplete(fresh endpoint-search scratch per negotiation, absent lower-bound cost revision, atomic catalog rebuild, duplicate baseline/progress copies, full domain rebuild after acceptance; section 2 product gate met at 20.5 s so these are not gate blockers) |
| A.3 | `lib/Mapping/Tech/TechMappingSearch.cpp` (`constructiveSearchSurfaceThreshold`, `ConstructiveCoverSearch`), docs/spec-pnr.md "TechMapping Hardware Demand Feedback", docs/spec-tech-mapping.md | code and both specs agree: the 4096 surface threshold is a stable algorithm-family switch independent of the resolved `partial_cover_expansion_limit`, the resolved budget bounds the constructive frontier (`localBudget`), every constructive branch returns (no fall-through into `ComponentCoverCursor`), and a limited frontier is `ProofNotEstablished`; lit `test/mapping/tech_mapping_search.test` | closed (semantic decision: keep the constant family switch; the spec was amended after the 0825 audit and the code matches it) |
| A.7 | `SpatialPnrGenerator.cpp` (`serialPrefix`, `FirstVerifiedCandidate`) | `test/dse/root_complete_spatial_pnr_first_verified.test`; spec: "FirstVerifiedCandidate remains a serial bounded-prefix execution" | deferred-spec(docs/spec-pnr-provider-builtin.md "Spatial Restart Sequence": intentionally serial; a speculative prefix window is a new policy, not exhaustive execution) |
| A.8 | `SpatialPnrGenerator.cpp` worker allocation (restart count, active route-graph units, CPU claim, calibrated memory) | invocation statistics `worker_count` and the resource claim in the run | deferred-spec(no enclosing invocation worker budget or shared executor exists; needs a spec owner for the invocation-level budget before code) |
| recur-1 | `lib/PnR/SpatialRecurrenceTiming.cpp`; `MappingObjectiveProgram::selectsMeasure` (builtin profiles skip the dead projection) | builtin Spatial profiles never select recurrence in the run (no `recurrence` measure in `objective_codes` projection) | deferred-performance(custom profiles only: predecessor reconstruction and scratch reuse for witness paths; no builtin gate affected) |
| noop-1 | `SpatialAnnealingSearch.cpp` (`isIdentitySpatialAction` before the transaction for binding, tag, and resource re-selections, drawing no acceptance randomness; `SpatialActionProbe::isSemanticNoop` after `routeAffectedNets` and `close`) | quiet-host depth-16 VC run: 97,999 of 262,526 probes (37.3 %) are semantic no-ops, `cached_inactive_actions` 51, so the residual no-ops are transport routing re-selections whose re-route reproduces the incumbent tree and are known only after the route (`annealing_level.semantic_noop_actions` per level, `c3/quiet-off-branch-depth16-vc-1`) | deferred-performance(baseline 37.3 % of probes; the safe pre-transaction subset is landed; the remaining lever is a proposal-domain predicate that excludes route re-selection of nets whose incumbent is already a shortest path under the current negotiated costs, which changes the proposal schedule and therefore needs a Spatial config descriptor bump plus corpus A/B before it can land) |
| ripup-1 | `SpatialActionExecutorScratch::markNet`/`markWholeNet`; `SpatialNetRouterScratch::planNegotiatedRoute` (Preserve / SelectedSinks / WholeNet) | PathFinder closure statistics `preserved_net_routes`, `selected_sink_routes`, `whole_net_routes` in the run | deferred-performance(placement/dependency closure still selects WholeNet; reuse the existing pruning where changed terminal domains permit) |
| domain-1 | `SpatialActionDomainScratch::rebuild` after accepted Actions (`lib/PnR/SpatialActionDomain.cpp`) | `annealing_level.accepted_actions` per level (one rebuild each) and perf share | deferred-performance(delta maintenance must preserve canonical action order and inactive-cache invalidation) |
| snap-1 | `SpatialAnnealingSearch.cpp` incumbents: `snapshotFullyRouted` at capture, `materializeFullyRouted` once per restart (ec8078e48) | `annealing_level.incumbent_snapshots`; restart restores exactly one incumbent (`bestFeasibleIncumbentRestored`) | closed (capture is a read-only snapshot; the single materialization replays and verifies by design to keep the `codes()` cold cross-check) |
| astar-1 | `EndpointRouteSearchScratch::search` per-query validation of the arc cost arrays (`arcCostsAlreadyValidated` over the typed `EndpointRouteInputRevision` pair since 1887e6949, after the audit base 0188e26e1; `physicalTimingAlreadyValidated` for the timing arrays) | per-level `arc_cost_validation_scans` and `physical_timing_validation_scans` emitted since `f35cb0d86`: 514,762 scans over 46 levels of `c3/ab1-lever-depth16-vc-1`, 4.97 per accepted action and 1.27 per heuristic lookup, so the revision cache is ineffective because `SpatialRouteCostState` advances the current-cost revision on every synchronized probe; `search` 9.9 % inclusive / 1.3 % self in the call-graph window | deferred-performance(the typed revisions exist and are observable; the scan can only leave the query path when the cost owner certifies finiteness and `current >= lower` for the arcs it writes and the search trusts the certified revision pair; that owner-side certification is the next lever and keeps the validation, moving it to the writer) |
| nego-1 | `SpatialPathFinderRouterScratch::analyzeCapacityConflicts` (`emitDecisions` gate on `cutSeenTraversals_`/`cutSeenEndpoints_`) | code: the seen arrays are cleared and marked only under `mapping_debug::Level::Decision`; the summary-level run never touches them; `derivedUsage != usage` oracle retained | closed (zero-risk subset landed; the route-usage reconstruction is the retained independent oracle) |
| nego-2 | RouteTree self-verification and full projection on negotiation paths (`SpatialMoveTransaction::projectCurrentRoutes`, `RouteTreeState` verify) | perf share of projection in `perf-top.txt` | deferred-performance(any removal must keep candidate-level cold verification and exact usage equality) |
| init-1 | `SpatialCandidateInitializer.cpp` topology distances (`fillHopDistances`) | `spatial_restart_report.seed_ns` per restart in the run | deferred-performance(seed construction is a sub-second fraction of a restart; invocation-local memoization is the safe subset, deterministic work reproduced exactly) |
| tech-2, tech-5, tech-6, tech-7, tech-8, tech-1-safe | `lib/Mapping/Tech/TechMappingSearch.cpp` (compute family construction, byte-key comparisons, context-domain work, store re-import, supply rechecks) | tech-mapping phase wall (`phase_ledger` in `stdout.json`) and perf samples under `TechMapping` symbols in `perf-top.txt` of the run | deferred-performance(re-profiled on this branch; the cross-state incremental matching stays rejected; next levers in order: canonical memory-supply interning, span/CSR compute domains, epoch-stamped visited arrays, resumable selection enumeration under the family quota) |
| D6 | `lib/PnR/CpSat/CpSatExactProtocol.cpp` (`installCanonicalDecisionStrategy`), `SpatialExactRepair.cpp` | `spatial_restart_report.exact_repair_ns` in the run and `exact_repair` wall per CEGAR iteration (`solver_calls` 4-7) | deferred-performance(exact repair is sub-second per iteration; hints and tightened domains are not material on any current profile) |
| verify-1 | `FrozenSpatialPnrProblemBuilder::verifyAggregate` and per-index builders in `SpatialPnrProblem.cpp` | freeze wall per CEGAR iteration (see T1.3) | typed-incomplete(consolidation only after Fabric-only versus workload cross-check ownership is separated; publication and cold oracles stay) |
