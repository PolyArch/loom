# Review 0820-0825 closure ledger (G3), created 2026-09-02 before G3 start

Each row: semantic owner -> production call path -> focused evidence -> independent replay -> performance data -> exact status.
Status vocabulary: open | in-progress | closed | typed-incomplete(<reason>) | deferred-performance(<baseline>) | deferred-spec(<anchor>).

| Row | Item (from plan section 7) | Owner | Production call path | Focused evidence | Independent replay | Performance data | Status |
|---|---|---|---|---|---|---|---|
| E0 | Current-WIP regression replay: every 0823/0824 item that 0827 marked closed and whose owner is touched by the WIP (schema, Fabric handshake, Mapping artifact, PnR objective/config, |  |  |  |  |  | open |
| E1 | Wire `loom-structured-ownership-evaluation-test` into `LOOM_TEST_DEPENDS` and lit. |  |  |  |  |  | open |
| E2 | Derive `test/simulation_conformance.py:_FABRIC_SCHEMA` from the codec owner (7.1). |  |  |  |  |  | open |
| E3 | Tracked ten-profile CGRA simulation gate input with its generator (`qualify_cgra_budget.py`) owned by build/lit. |  |  |  |  |  | open |
| E4 | Decide and implement the `FabricArtifactBytecode` producer string `loom.fabric.3.0` in canonical identity bytes. |  |  |  |  |  | open |
| E5 | Reader for `unsupportedMemoryContract` evidence or removal of the field. |  |  |  |  |  | open |
| E6 | Spec/rationale synchronization for every schema bump in the G2 commits and the twelve documents named by 0822-0. One-run evidence items: |  |  |  |  |  | open |
| E7 | FPA finalist join: Attention + PageRank rows with `--loom-fpa-weight-root`, asserting `hardware_promotion_observations[].promoted_to_exact_mapping`. |  |  |  |  |  | open |
| E8 | Real OpenROAD leaf campaign (offline, four-hour budget, weight <= 10 GB) with `/mnt/nas0/software/openroad/2026.08.25-*` and gpdk045 through `buildFpaGroundTruthCollectionPlan`, or |  |  |  |  |  | open |
| E9 | Product spectrum artifact through `verify_product_visualization.py --expected-spectrum-class --require-transition --minimum-region-count` on context-24 Attention plus one non-Atten |  |  |  |  |  | open |
| E10 | Evidence manifest regeneration from a clean checkout, join status restated against the owner-scoped status plus run-key digest. |  |  |  |  |  | open |
| E11 | Full-suite evidence publication in a tracked location or checkpoint note. Implementation items (time-boxed, parallel on disjoint owners): |  |  |  |  |  | open |
| E12 | Live-state migration domain: typed state-correspondence records, live-state classes, nonzero migration/reprogramming cost accounting, application replay with one nonempty live-state transition and a typed negative. | `lib/PnR/System/ResourceTimeTransition.cpp` (finalizer derives `ResourceTimeLogicalMemoryCorrespondence` per Canonical Dataflow memory root; typed `ResourceTimeTransitionRefusal` reasons `ordered_channel_state`, `dynamic_work_state`, `logical_memory_unbound`, `logical_memory_relocated`, `logical_memory_reinitialized`, `hardware_programming_changed`, `completion_frontier_inadmissible`); `include/PnR/System/SystemMappingMigration.h` (closed `ResourceTimeLiveStateClass`, `ResourceTimeLiveStateMigration`); `lib/Runtime/ResourceTimeTransitionSelection.cpp` (admits only retained-in-place records at zero cost); codecs in `RuntimeManifest.cpp` (4.0), `Package.cpp`, `ProductVisualization.cpp` (bundle 1.3), `ResourceTimeProjection.cpp`, `ResourceTimeExecution.cpp`; specs `docs/spec-dse-feedback.md`, `docs/spec-pnr.md`, `docs/spec-runtime-abi.md`; commit `da969b233` | loom-cc application build -> `ResourceTimeSpectrum.cpp` finalizes every Mapping-path edge through `finalizeResourceTimeTransition` (migration time = sum of derived records, refusals typed); `DeploymentBuild.cpp` publishes the graph in the 4.0 runtime manifest; `loom-system-run` -> `loadApplicationDeployment` -> `ResourceTimeTransitionSelectionSession::create` admits only retained-in-place edges | `temp/spectrum/e12/deployment-closure.log` (verified two-root edge, authored-correspondence refusal, migration-time disagreement refusal, 48.5 s); `temp/spectrum/e12/candidate-state-graph-binding.log`; `temp/spectrum/e12/attention/build.stderr` and `execution/manifest.json` (Attention smoke: `resource_time_transition_proof_failures 0`, entry-only graph, 8 acknowledged events); `temp/spectrum/lit/focused-1.log` (deployment, runtime, dse, pnr cells 48 of 49; the one failure is the load-sensitive device-level gem5 runtime cell noted under E13) and `temp/spectrum/lit/visualization-1.log` (bundle 1.3 cells pass) | `build/test/deployment/loom-deployment-closure-test`; `build/test/pnr/loom-system-candidate-state-test graph-binding`; `llvm-lit -sv --filter=deployment/ build/test`; `llvm-lit -sv --filter=system/product_external_hardware build/test`; `temp/spectrum/e12/attention/run.sh` | closure test 48.5 s / 141 MB; candidate-state graph-binding 15.2 s / 162 MB; Attention loom-cc 7:11 wall / 1.67 GB and loom-system-run 1:17 / 1.04 GB on a host at load average 70-110 (other slices building); focused lit 5:39 at 6 workers | typed-incomplete(application_replay_pending_multi_endpoint_path): the typed domain, derivation, refusal reasons, selector admission, codecs, and specs are closed and exercised by the closure verifier and every production build; no portfolio application yet produces a multi-endpoint Mapping path (Matmul and Attention smoke are entry-only), so the application replay of a nonempty retained-in-place edge and its typed negative wait for the E9 product spectrum artifact; a relocated memory stays a typed refusal because no migration executor exists; nonzero reprogramming time stays refused until an exact programming-time owner exists |
| E13 | Synchronous safe-point drive: a production caller for `prepareGem5SystemCompletionControlledInvocation`. | `lib/Runtime/Gem5RootEventControl.cpp` (`deriveGem5RootEventEndpointTable`, `Gem5RootEventController`), `lib/Runtime/Gem5SystemExecution.cpp` (typed `Gem5RootEventEndpointTable`), `lib/Application/DeploymentRuntime.cpp` (`LoadedApplicationDeployment::driveGem5RootEvent`), `tools/loom-system-run/loom-system-run.cpp` (controlled DFG cell); specs `docs/spec-runtime-abi.md`, `docs/spec-loom-stack.md`; commit `5b719b4fb` | `loom-system-run` -> `prepareResourceTimeDrive` (endpoint table from the manifest graph, prepared selector loaded before the cell) -> `execute(Engine::Dfg, ..., drive)` -> `prepareGem5SystemCompletionControlledInvocation` + `bindPreparedEvaluationModelInvocation` -> `Gem5RootEventController::listen` -> fresh bundle execution with every root event acknowledged through `driveGem5RootEvent` -> `validateAcknowledgedRootLifecycle` -> trace published from the synchronous session; the post-run lifecycle feed is removed | `temp/spectrum/e13/matmul/execution/manifest.json` (`resource_time_drive` = synchronous, endpoint_count 1, acknowledged_event_count 2, checksum 1219 verified) and `bundles/system-dfg/outputs/system-root-lifecycle.result` (device records with acknowledgement generations 1 and 2, decisions Continue and Stay at endpoint 0) versus the uncontrolled CGRA cell (generation 0); `temp/spectrum/e12/attention/execution/manifest.json` (synchronous, 8 acknowledged events); `temp/spectrum/lit/system-1.log` (product cells matmul, vecadd-memory, branch-select, multicast-partition pass through the controlled drive; the device-level `runtime/gem5_system_execution.test`, untouched by this slice, failed twice and passed once under host load 40-110 with the device reporting an activation while a dispatch record was still active, recorded as a pre-existing load-sensitive cell for the System runtime owner) | `temp/spectrum/e13/matmul/run.sh` (loom-c++ Matmul with `product_attention_dse_grown.yaml`, then `loom-system-run --deployment-package=... --gem5-readiness=build/gem5/loom-gem5-readiness.json --expected-i32=1219`); decode `system-root-lifecycle.result` as 4-byte magic plus 56-byte big-endian records (entity u64, occurrence u64, action u32, tick u64, delta u64, acknowledgement generation u64, decision u32, endpoint u64) | loom-c++ 2:55 wall / 1.21 GB and loom-system-run (DFG + CGRA cells) 2:34 wall / 1.02 GB on a host at load average 70-110; DFG cell terminal tick 51.2 M ps, CGRA 590.7 M ps; the controller adds two socket round trips per root event | closed (typed residual: `loom-system-run.cpp` is 2220 lines and its drive composition should move with the E22 mapped-RTL extraction; the endpoint table admits only terminal edges because a nonzero endpoint has no dispatch targets, so a child that must launch further roots needs the complete endpoint target projection in the gem5 projection and device; no portfolio application yet has an edge, so `ActivateEndpoint` is exercised only by the device-level `scripts/test_loom_gem5_runtime.py` cell) |
| E14 | Dedicated `SpatialTopology`, `InstructionCapacity`, `SystemInstructionContext` mutation families with durable per-family `FinalizedDeployment`/`ResourceTimeTransition` records. | `lib/DSE/HardwareMutationRepairRecord.cpp` (`loom.dse.hardware_mutation_repair_record` 1.0 codec, strict import), `lib/DSE/JointHardwareRuntimeRepair.cpp` (`executeJointHardwareMutationRepair` publishes the record; topology overload of `materializeJointModuleHardwareMutation`), `lib/DSE/JointHardwareReopenCandidate.cpp` (typed Module topology growth), `test/dse/JointDesignExplorationTest.cpp` (three dedicated families with declared disposition contracts), `docs/spec-dse-feedback.md`; commit `afa63f545` | loom-cc -> `MappingExecution.cpp` -> `executeJointRuntimeWitnessRepair` -> `executeSpatialFifoHardwareFeedbackReopen` (typed Module FIFO capacity and bypass children) and `executeSpatialOperandBufferHardwareFeedbackReopen` -> `executeTypedModuleHardwareReopen` -> `executeJointHardwareMutationRepair` -> `publishHardwareMutationRepairRecord` (record identity named in the `joint_hardware_mutation_repair` observation collected by `scripts/generate-loom-evidence-manifest.py`); the FIFO queue-discipline reopen of the A2 StrictFifo case resolves its recipe variable through the recipe growth path (`mapping_feedback_recipe_growth`, `spatial_fifo_hardware_repair` observation) and therefore executes no `JointHardwareMutationRepair` and publishes no record | `temp/spectrum/e14/spatial-topology.stdout`, `instruction-capacity.stdout`, `system-instruction-context.stdout`, `fu.stdout` (record identities per family: spatial-topology cold_fallback/cold_fallback, instruction-capacity cold_fallback/cold_fallback, system-instruction-context preserved/reopened, fu cold_fallback/cold_fallback) and the matching `.stderr` observations; `temp/spectrum/lit/focused-1.log` (all 14 `joint_design_mutation_*` cells pass with the record key required); `temp/spectrum/e14/a2-reopen/build.stderr` (production A2 reopen replay on this branch: child System 6cf6c75b..., but the queue-discipline recipe path emits no record, confirming the residual below) | `env LOOM_VERBOSE_LEVEL=1 build/test/dse/loom-dse-joint-design-exploration-test mutation-matrix=spatial-topology` (likewise `mutation-matrix=instruction-capacity` and `mutation-matrix=system-instruction-context`); `llvm-lit -sv --filter=dse/joint_design_mutation_ build/test`; `temp/spectrum/e14/a2-reopen/run.sh` | per family 28-31 s wall / 445-470 MB (cold plus preserve-first Tech/Spatial/System PnR and independent verification of both sides); 14 cells in the focused lit run at 6 workers; A2 production reopen replay 17:17 wall / 6.0 GB max RSS on a host at load average 40-70 (baseline 7:08 unloaded) | typed-incomplete(deployment_record_not_applicable): the three families execute through the production repair owner with durable per-family records carrying every affected cone, typed dispositions, accounting, cold and preserve-first Mapping roots, dispatch and verifier accounting, and quality observations; a hardware mutation child lives on a different System, so a `ResourceTimeTransition` (same-Fabric contract) is structurally not its record and a `FinalizedDeployment` exists only when the application build selects the child System (`hardware_dse_alternative` pair decision, A2 evidence); residuals: InstructionCapacity and SpatialTopology witness the cold fallback because the local Module correspondence maps neither instruction-context inventory nor topology entities; the recipe-growth reopen (FIFO queue discipline, uniform Tech growth) still bypasses the mutation repair executor and its record, so the next lever is routing `deriveHardwareRecipeGrowth` children through `executeJointHardwareMutationRepair`; the record lives in the invocation ArtifactStore and the package closure does not yet reference it |
| E15 | DynamicWork production join: frontend emission, stable `WorkItemId` projection, application caller, deterministic replay, typed refusal. |  |  |  |  |  | open |
| E16 | CGRA atomics/volatile/fence fallback decision path with one verified fallback replay per admitted contract class. |  |  |  |  |  | open |
| E17 | Ordered-channel System-level lifecycle (generations/epochs, bounded host LLJIT capacity, cancellation, negative rate case) under one application identity. |  |  |  |  |  | open |
| E18 | Polyhedral tiling/strip-mining/parallel/reduction proposal production and a polyhedral-to-System replay artifact. |  |  |  |  |  | open |
| E19 | Funnel exact-comparison sample against real PnR with `OutOfDistribution` assignment. |  |  |  |  |  | open |
| E20 | Portfolio joined verdict (`evaluate_portfolio` wired), full QoR dimensions (six of eleven null), TinyML product profile beyond 0-warmup/1-measured, workload/runtime-input identity  |  |  |  |  |  | open |
| E21 | gem5 System-simulation host-side performance (0825 analysis, complete contract): per-phase wall / self CPU / child CPU for buildGem5Binding, prepare, external execution, import, Ev |  |  |  |  |  | open |
| E22 | Responsibility decisions, documented per file with a cohesion argument where no extraction is made, for the thirteen production files above 2,000 lines, the three test files above  |  |  |  |  |  | open |
| E23 | PnR deep-review residual rows from `temp/loom-pnr-deep-review-0825-current-audit.md`, each with owner, production call, evidence artifact, replay command, and typed residual: T0.3, |  |  |  |  |  | open |
